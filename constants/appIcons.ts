import { LucideIcon, Home, User, Settings, ShoppingCart } from 'lucide-react';

export const iconMap: Record<string, LucideIcon> = {
  home: Home,
  user: User,
  settings: Settings,
  shop: ShoppingCart,
  // outros ícones
};
